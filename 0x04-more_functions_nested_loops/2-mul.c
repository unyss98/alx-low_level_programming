#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: input number
 * @b: input number
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int num;

	num = a * b;
	return (num);
}
